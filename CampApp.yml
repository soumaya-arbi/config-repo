server:
  port: 8070

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campapp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
